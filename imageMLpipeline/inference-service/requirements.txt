kafka-python==2.0.2
minio==7.2.9
Pillow==10.4.0
psycopg2-binary==2.9.9

# Anomaly-detection (ResNet + FAISS) inference stage
numpy==1.26.4
onnxruntime==1.18.1
faiss-cpu==1.8.0
matplotlib==3.9.2
torch==2.3.1
torchvision==0.18.1
# NOTE: plain torch/torchvision from PyPI pull the large CUDA build. For a slim
# CPU-only image, install them from the CPU wheel index instead:
#   pip install torch==2.3.1 torchvision==0.18.1 --index-url https://download.pytorch.org/whl/cpu

# Stage-two defect clustering (ResNet50 / DINOv2 -> descriptors -> UMAP -> HDBSCAN).
# All pins ship cp311 manylinux or pure-python wheels — python:3.11-slim has no
# compiler. The whole-set clustering is fit at batch time (no version-coupled
# joblib artifacts); scikit-image supplies the colour/shape mask descriptors.
scikit-learn==1.5.2
scikit-image==0.24.0
scipy==1.13.1
joblib==1.4.2
numba==0.60.0
llvmlite==0.43.0
pynndescent==0.5.13
umap-learn==0.5.7
hdbscan==0.8.40

# Stage-one scoring is dispatched to the Spark cluster; the service hosts the
# driver in client mode. Must match the cluster's Spark version exactly — the
# spark-app image installs the same pin. predict_batch_udf arrived in 3.4.
pyspark==3.4.0
# predict_batch_udf is an Arrow-backed pandas UDF, so both ends need pandas and
# pyarrow — same pins as the spark-app image, which already installs them.
pandas==2.2.2
pyarrow==16.1.0
